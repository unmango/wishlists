apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wishlists.fullname" . }}-test-connection"
  labels:
    {{- include "wishlists.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wishlists.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
